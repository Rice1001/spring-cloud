server:
  port: 8086
spring:
  cloud:
    config:
      name: configtest
      profile: dev
      label: master
      discovery:
        enabled: true
      uri: http://localhost:8085/
  application:
    name: configtest
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/

