server:
  port: 8086
spring:
  cloud:
    config:
      name: configtest
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: config-server
    bus:
      trace:
        enabled: true
  application:
    name: configtest
  rabbitmq:
    host: 127.0.0.1
    port: 5672

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/



