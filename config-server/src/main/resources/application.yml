server:
  port: 8085
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Rice1001/spring-cloud-config
          search-paths: config-repo
          username: Rice1001
          password: 1998403Rice
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka